meta {
  name: 003_Verify_Mappings
  type: http
  seq: 3
}

get {
  url: {{baseURL}}winlogbeat-000001/_mapping
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("mappings response verification",function(){
    let json = require('./mappings_response.json').toString().replace(/\s+/g,'');
    var data = res.getBody().toString().replace(/\s+/g,'');
    expect(data).to.equal(json);
    });
}
