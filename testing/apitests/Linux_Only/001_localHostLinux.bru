meta {
  name: 001_localHost
  type: http
  seq: 1
}

get {
  url: {{baseURL}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.name: eq es01
}

tests {
  test("Status code is 200",function(){
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
